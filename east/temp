    # notiHour, notiMin = noti_time.split(":")
    

    # for disturb in do_not_disturb:
    #     startDis, endDis = disturb.split("~")
    #     startDisHour, startDisMin = startDis.split(":")
    #     endDisHour, endDisMin = endDis.split(":")
        
    #     if endDisHour < startDisHour or ( startDisHour == endDisHour and endDisMin < startDisMin):
    #         if notiHour < endDisHour or ( notiHour == endDisMin and notiMin < endDisMin):
    #             notiHour, notiMin = endDisMin, endDisMin
    #         elif startDisHour < notiHour or (startDisHour == notiHour and startDisMin < notiMin)
    #             notiHour, notiMin = endDisMin, endDisMin
    #     elif 